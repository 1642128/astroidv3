image: atlassian/default-image:3

pipelines:
  default:
    - step:
        name: 'Deploy to S3'
        script:
          - pip install --upgrade pip
          - pip install awscli
          - aws s3 sync public/ s3://theresaholeinthebucketdearlayza/ --region us-east-1 --delete
